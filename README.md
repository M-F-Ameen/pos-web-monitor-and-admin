# POS Monitoring System

Multi-tenant POS monitoring system with a cloud API server and web dashboard.

## Structure

- **`server/`** — Express + Socket.io backend with PostgreSQL (deployed on Railway)
- **`frontend/`** — Next.js admin dashboard (deployed on Railway)
