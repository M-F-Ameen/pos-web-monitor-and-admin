/**
 * Default product/category image — imported via Vite so the path
 * is resolved correctly in both dev and production builds.
 */
import defaultImageUrl from "./images/default product image/default-mage.jpg";

export const DEFAULT_IMAGE: string = defaultImageUrl;
